apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Prometheus Operator와 대상 Controller가 준비된 뒤 동기화한다.
# 개별 리소스의 namespace를 유지한다. 전역 namespace 변환을 넣지 않는다.
resources:
  - ../backend
  - ../platform
  - ../alerts/rules
  - ../logs/policies
