apiVersion: v1
kind: Service
metadata:
  name: ai-worker
  namespace: ai
  labels:
    app.kubernetes.io/name: ai-worker
    app.kubernetes.io/part-of: jangingmall
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-worker
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
