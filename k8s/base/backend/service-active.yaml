apiVersion: v1
kind: Service
metadata:
  name: backend-active
  namespace: app
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: jangingmall
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
